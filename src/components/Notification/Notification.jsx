import styles from "./Notification.module.css";

const Notification = () => (
  <div className={styles.container}>
    <p>Feedback has not been provided yet.</p>
  </div>
);

export default Notification;